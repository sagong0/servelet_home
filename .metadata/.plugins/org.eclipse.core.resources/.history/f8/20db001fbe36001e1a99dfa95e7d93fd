function check(){
	if(f.mid.value==""){
		alert("사용자 아이디를 입력해주세요.")
	}else{
		ajaxfunc(f.mid.value);
	}
}

let request;
let arrayFromJson;

function ajaxfunc(idData){
	var ajax_data = "mid="+idData;
}